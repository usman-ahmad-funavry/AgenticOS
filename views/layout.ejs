<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title %> | Dashboard
  </title>
  <link rel="stylesheet" href="/style.css">
  <!-- Add Font Awesome for icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body class="bg-gray-50 flex min-h-screen">
  <!-- Sidebar -->
  <div class="w-64 bg-gradient-to-b from-blue-800 to-blue-900 text-white shadow-xl">
    <div class="p-6 text-center">
      <h1 class="text-2xl font-bold tracking-tight">Dashboard</h1>
      <div class="mt-2 text-blue-200 text-sm">Welcome back!</div>
    </div>
    <nav class="mt-6">
      <ul class="space-y-2 px-4">
        <li>
          <a href="/"
            class="flex items-center px-4 py-3 text-gray-100 hover:bg-blue-700 rounded-lg transition-colors duration-200 <%= path === '/' ? 'bg-blue-700' : '' %>">
            <i class="fas fa-calendar-days w-5"></i>
            <span class="ml-3">Scheduler</span>
          </a>
        </li>
        <li>
          <a href="/live-news"
            class="flex items-center px-4 py-3 text-gray-100 hover:bg-blue-700 rounded-lg transition-colors duration-200 <%= path === '/live-news' ? 'bg-blue-700' : '' %>">
            <i class="fas fa-newspaper w-5"></i>
            <span class="ml-3">Live News</span>
          </a>
        </li>
        <li>
          <a href="/api/login"
            class="flex items-center px-4 py-3 text-gray-100 hover:bg-blue-700 rounded-lg transition-colors duration-200">
            <i class="fas fa-rotate w-5"></i>
            <span class="ml-3">Refresh Token</span>
          </a>
        </li>
      </ul>
    </nav>
  </div>

  <!-- Main Content -->
  <div class="flex-1">
    <!-- Top Navigation Bar -->
    <header class="bg-white shadow-sm">
      <div class="flex items-center justify-between px-8 py-4">
        <img src="/images/logo.svg" alt="ChainGPT" class="w-[10rem]">
      </div>
    </header>

    <!-- Page Content -->
    <main class="p-8 bg-gray-50">
      <%- body %>
    </main>
  </div>
</body>

</html>